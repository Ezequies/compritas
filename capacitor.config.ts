import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'compritas-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
